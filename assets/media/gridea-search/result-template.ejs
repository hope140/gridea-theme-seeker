<div class="post-list-container">
  <div class="post-inner">
    <% posts.forEach(function(post) { %>
      <div class="post">
        <div class="post-left">
          <a href="<%= post.link %>">
            <div>
              <!-- <a href="<%= post.link %>"> -->
              <span class="post-title"><%= post.title %></span>
              <!-- </a> -->
            </div>
            <div class="post-content post-abstract text-gray-700 font-light my-4">
              <p><%- post.searchedPreview %></p>
            </div>
          </a>
          <div class="post-info">
            <time class="post-time">
              · <%= post.dateFormat %> ·
            </time>
            <% post.tags.forEach(function(tag) { %>
              <a href="<%= tag.link %>" class="post-tag">
                # <%= tag.name %>
              </a>
            <% }); %>
          </div>
        </div>
        <% if (themeConfig.showFeatureImage && post.feature) { %>
          <a href="<%= post.link %>" class="post-feature-image" style="background-image: url('<%= post.feature %>')"></a>
        <% } %>
      </div>
    <% }); %>
  </div>
</div>
